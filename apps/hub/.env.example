# Copy to .env and fill in with the same Storage bucket used by
# apps/editor/.env — a public client identifier, not a secret. The hub
# fetches games/index.json directly from this bucket's public download
# URL (see src/games-storage.js), no Firebase SDK involved. Without this,
# the hub just shows "No games published yet."
VITE_FIREBASE_STORAGE_BUCKET=
